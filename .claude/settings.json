{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "for f in $CLAUDE_FILE_PATHS; do case \"$f\" in *.md|*.mdx) command -v markdownlint-cli2 >/dev/null && markdownlint-cli2 --fix \"$f\" 2>/dev/null || true ;; esac; done"
          }
        ]
      }
    ]
  }
}
